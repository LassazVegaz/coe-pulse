# Data Processing

This sub project is to analyze datasets. My first choice was `NParksTracks`. This dataset is only useful if a map is shown in the frontend - which is complicated and time consuming. Hence I switched to `COEBiddingResultsPrices`. This dataset has enough numerical values that can derrive couple of meanigful grapghs.
